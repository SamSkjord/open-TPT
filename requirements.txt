# openTPT Requirements
# Core dependencies for Raspberry Pi operation

# Display and GUI
pygame>=2.0.0

# Hardware interfaces
adafruit-blinka>=8.0.0

# I2C multiplexer (TCA9548A) - for accessing multiple sensors
adafruit-circuitpython-tca9548a>=0.7.0

# ADC (ADS1115) - for brake temperature IR sensors
adafruit-circuitpython-ads1x15>=2.2.0

# Thermal sensors
# Note: MLX90640 used by Pico I2C slaves (not directly by Pi)
# Pi reads processed data from Picos via I2C slave interface
adafruit-circuitpython-mlx90640>=1.2.0  # Optional: for setup/diagnostic tools
adafruit-circuitpython-mlx90614>=1.2.0  # Single-point IR sensors (when USE_MLX90614_SENSORS=True)

# NeoPixel buttons
adafruit-circuitpython-seesaw>=1.12.0
adafruit-circuitpython-neopixel>=6.3.0

# Numerical computing
numpy>=1.19.0

# Performance optimizations (optional but recommended)
numba>=0.56.0  # JIT compiler for thermal zone processing

# Computer vision (for camera)
opencv-python>=4.5.0

# CAN bus support (for future radar/OBD modules)
python-can>=4.0.0

# GPS support (for future lap timing)
# pynmea2>=1.18.0
# pyserial>=3.5

# Data logging (for future telemetry logging)
# pyarrow>=10.0.0  # For Parquet export

# Testing and development
# pytest>=7.0.0
# pytest-cov>=4.0.0
